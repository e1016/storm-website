import React from 'react'

const Storage = () => (
  <section className="main--info-section">
    <h1>Storage</h1>
    <p>
      Storm is not the substitute for a database, Storm is designed to help in the structuring of temporal data, the approximate limit of localStorage in browsers is about 5 ~ 10MBs, it is not recommended to save files with encode in base64.
    </p>
  </section>
)

export default Storage

import React from 'react'

const Storage = () => (
  <section className="main--info-section">
    <p>
      Storm is not the substitute for a database, Storm is designed to help in the structuring of data, the approximate limit of localStorage in browsers is about 5MBs, it is not recommended to save files with encode in base64
    </p>
  </section>
)

export default Storage